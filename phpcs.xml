<?xml version="1.0"?>
<ruleset name="WP Smart Slug">
    <description>Basic coding standards for WP Smart Slug plugin</description>

    <!-- Files to check -->
    <file>includes</file>
    <file>admin</file>
    <file>wp-smart-slug.php</file>
    
    <!-- Exclude paths -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*/assets/*</exclude-pattern>

    <!-- Use basic PSR standards -->
    <rule ref="PSR1"/>
    <rule ref="PSR2">
        <!-- Allow WordPress tab indentation -->
        <exclude name="Generic.WhiteSpace.DisallowTabIndent"/>
    </rule>

    <!-- Add command line arguments -->
    <arg name="extensions" value="php"/>
    <arg value="ps"/>
    <arg name="colors"/>
    <arg name="parallel" value="8"/>
</ruleset>